{{- if or .Values.ton.enabled .Values.ton_usdt.enabled}}
---
apiVersion: v1
kind: Service
metadata:
  name: ton-shkeeper
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ton-shkeeper
  ports:
  - name: app-tcp
    port: 6000
    protocol: TCP
{{ end }}
